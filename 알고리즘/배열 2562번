#include <stdio.h>

int main(void) {
  int max=0;
  int num[9];
  int count=0;

  for(int i=0; i<9; i++)
  {
    scanf("%d\n", &num[i]);
    if(max<num[i])
    {
      max = num[i];
      count = i+1;
    }
  }
  printf("%d\n%d", max, count);
}
